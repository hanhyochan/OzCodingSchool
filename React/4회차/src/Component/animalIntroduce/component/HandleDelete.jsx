import React from 'react';
import { saveToLocalStorage, getFromLocalStorage } from "./localStorage";

const HandleDelete = (fomrList) => {
    console.log(fomrList)
    return (
        <div>
            
        </div>
    );
};

export default HandleDelete;